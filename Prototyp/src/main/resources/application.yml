spring:
  datasource:
    password: password
    username: user
    url: jdbc:postgresql://localhost:5432/elfes
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB